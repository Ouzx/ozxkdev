import React from "react";
import styles from "./Logo.module.scss";
const Logo = () => {
  return (
    <a className={styles.logo} href="/">
      <h2>ozxk.dev</h2>
    </a>
  );
};

export default Logo;
